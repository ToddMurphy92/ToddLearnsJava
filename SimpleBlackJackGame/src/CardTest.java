/**
 * Created by todd on 4/3/17.
 */
public class CardTest {

    public static void main (String [] args) { /*

        DeckOfCards deck = new DeckOfCards();   // Creates the deck
        deck.shuffle();                         // Shuffles the deck
        Hand player = new Hand();               // Creates players hand
        Hand house = new Hand();                // Creates house hand

        player.dealHand();      // Deal player hand
        house.dealHand();       // Deal house hand

        player.handValue();     // Determine player handScore
        house.handValue();      // Determine house handScore
        
        // Determine the winner
        if (player.handScore > house.handScore) {
            System.out.println("Player wins! " + player.handScore + "to" + house.handScore);
        }
        else if (player.handScore < house.handScore) {
            System.out.println("House wins!" + house.handScore + "to" + player.handScore);
        }
        else {
            System.out.println("Draw!" + player.handScore + "to" + house.handScore);
        } // Finish determining winner
*/
    }

}
